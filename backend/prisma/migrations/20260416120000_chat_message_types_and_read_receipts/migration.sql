-- Restored placeholder: migration file was missing locally and blocked `prisma migrate deploy`.
-- Current `schema.prisma` does not define message types / read receipts on `project_messages`.
-- If those columns are added later, create a new migration to ALTER TABLE.
SELECT 1;
