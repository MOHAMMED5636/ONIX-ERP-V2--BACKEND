import { Router } from 'express';
import { authenticate } from '../middleware/auth.middleware';

const router = Router();

// TODO: Implement document routes
// router.post('/upload', authenticate, uploadMiddleware, documentsController.uploadDocument);
// router.get('/:id', authenticate, documentsController.getDocument);
// router.get('/', authenticate, documentsController.listDocuments);
// router.delete('/:id', authenticate, documentsController.deleteDocument);

export default router;

